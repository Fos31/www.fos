jalankan
lihat file package.json dibagian
  Debug
  "scripts": {
  "dev": "vite", -> ketik pada terminal: npm run dev
  "build": "vite build",
  "preview": "vite preview",
  "clean": "rm -rf dist server.js",
  "lint": "tsc --noEmit"